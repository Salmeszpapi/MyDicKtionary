<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MyDicKtionary.View.WelcomeView"
             Title="WelcomeView">
    <VerticalStackLayout>
        <Label 
            Text="Welcome to .NET MAUI!"
            VerticalOptions="Center" 
            HorizontalOptions="Center" />
        <Button Command="{Binding StartQuizCommand}" 
                Text="Start Quiz"
                VerticalOptions="Center" 
                HorizontalOptions="Center" />
        <CollectionView ItemsSource="{Binding }">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <StackLayout Padding="10" Margin="5" BackgroundColor="LightGray">
                        <Label Text="{Binding English}" FontSize="18" />
                        <Label Text="{Binding Slovak}" FontSize="16" />
                        <Label Text="{Binding Hungarian}" FontSize="16" />
                    </StackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </VerticalStackLayout>
</ContentPage>